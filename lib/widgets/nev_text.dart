import 'package:flutter/material.dart';

class NavText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Swipe left to view graphs"),
    );
  }
}